% Label: Upper Bound

**Theorem (Upper Bound).** For every natural number $n$, if $n \geq 10^7$, then $\text{digitFactorialSum}(n) < n$, where $\text{digitFactorialSum}(n)$ denotes the sum of the factorials of the decimal digits of $n$.

*Proof.* Unsolved.